{
  "mcpServers": {
{{- if .searxng.url }}
    "searxng": {
      "command": "npx",
      "args": ["-y", "mcp-searxng"],
      "env": {
        "SEARXNG_URL": {{ .searxng.url | quote }}
      }
    }
{{- end }}
  }
}
