# Store passwords/cookies locally, never via a keyring/Secret portal.
# Prevents keyring unlock prompts and logout-on-reboot. Applies to every
# launch of helium-browser (the wrapper reads this file).
--password-store=basic
