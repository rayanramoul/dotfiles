screencopy {
  allow_token_by_default = true
  # Use the graphical picker (installed via hyprland-preview-share-picker-git)
  # so Discord/Zoom/Slack screen-share prompts show live monitor + window
  # thumbnails instead of a plain text list.
  custom_picker_binary = /usr/bin/hyprland-preview-share-picker
}
